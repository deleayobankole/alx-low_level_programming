#include <stdio.h>
/**
 * main - the code begins here
 *
 * Return: this will work if 0
 *
 */
int main(void)
{
	char xter;

	for (xter = 'z'; xter >= 'a'; --xter)
	{
		putchar(xter);
	}
	putchar('\n');
	return (0);
}
