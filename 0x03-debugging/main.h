#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

/*this function evaluates positive or negative values*/
void positive_or_negative(int i);

/*this function finds the largest of 3 values*/
int largest_number(int a, int b, int c);

/* function evaluates days left in the year */
int convert_day(int month, int day);

/* function to calculate days laeft in the year*/
void print_remaining_days(int month, int day, int year);

#endif /* MAIN_H */
